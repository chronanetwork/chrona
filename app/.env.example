# Solana RPC the dapp reads/writes (devnet by default)
NEXT_PUBLIC_RPC_URL=https://devnet.helius-rpc.com/?api-key=YOUR_KEY

# Scorer oracle base URL (GET /score/:wallet preview, POST /score signed)
NEXT_PUBLIC_SCORER_URL=http://localhost:8787
